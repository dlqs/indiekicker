extends ../_bootstrap
append styles
	// Custom styles for this template
	link(href='../css/starter-template.css', rel='stylesheet')
block body
	.container
		+navbar-fixed('indiekicker', 'navbar')
			li
				a Hello, #{session.name}
			+nav_item("/") Home
			+nav_item("/projects") All projects
			+nav_item("/user/" + session.userid, "active") My profile
			form.navbar-form.navbar-nav
				.form-group
					input.form-control(type='text', placeholder='Search projects')
				button.btn.btn-success(type='submit') Search
			form.navbar-form.navbar-right(method='post', action='/logout')
				button.btn.btn-primary(type='submit') Logout
	.container
		.starter-template
			h1 Bootstrap starter template
			p.lead
				| Use this document as a way to quickly start any new project.
				br
				|  All you get is this text and a mostly barebones HTML document.
	

